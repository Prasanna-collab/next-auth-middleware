import React from 'react'

interface Props {}

const page = () => {
  return <div>
    <h1>This componenet will not be routed. since the naming convention has _ in prefix.</h1>
  </div>
}

export default page